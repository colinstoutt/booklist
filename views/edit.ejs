<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Book List | New</title>
        <style>
            body {
                margin: 50px;
                font-family: sans-serif;
            }
            input {
                margin: 5px;
            }
        </style>
	</head>
	<body>
		<h1>Edit Book</h1>
        <!-- This line below is the only reason why this post route works -->
        <!-- We are making a POST to /books -->
		<form action="/books/<%= book._id%>?_method=PUT" method="POST" >
			Title: <input type="text" name="title" value="<%= book.title %>"/><br />
			Author: <input type="text" name="author" value="<%= book.author %>"/><br />
			Completed: <input type="checkbox" name="completed" <% if(book.completed
            === true){ %> checked <% } %>/><br />
            <!-- The default action of a form submit is to execute an http method -->
			<input type="submit" name="" value="Submit" />
		</form>
	</body>
	</body>
</html>
